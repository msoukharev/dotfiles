# Add some color to the terminal

alias grep="grep --color=auto"

if __macos; then
    export CLICOLOR=1
    alias ls="ls -G"
elif __linux; then
    export CLI_COLOR=1
    alias ls="ls --color=auto"
fi
